---
import MainLayout from "@layouts/layout.astro";
import Carousel from "@components/generic/carousel.astro";

const title = "Astro Chef";
---

<MainLayout title={title}>
  <div class="flex flex-col gap-5">
    <!-- TODO: add recipe cards here, ideally on a carousel -->
    <span class="text-text text-center text-5xl font-bold"> Random </span>
    <Carousel type="random" />
    {
      /*
      <span class="text-text font-bold text-center text-5xl">
        Popular
      </span>
      <Carousel type="popular" />
      */
    }
    <span class="text-text text-center text-5xl font-bold"> Liked </span>
    <Carousel type="random" filters={["liked"]} />
  </div>
</MainLayout>
