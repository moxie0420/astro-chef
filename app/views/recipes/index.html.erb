<% content_for :head do %>
<%= vite_stylesheet_tag "recipes.scss", "data-turbo-track": "reload" %>
<% end %>

<div id="recipes" class="recipe--list">
  <% @recipes.each do |recipe| %>
    <a href="/recipes/<%= recipe.id %>">
      <%= render "card", recipe: recipe %>
    </a>
  <% end %>
</div>
